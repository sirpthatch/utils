#!/usr/bin/env python3

import sys
import os

if __name__ == "__main__":
  for arg in sys.argv[1:]:
    renamed = arg.replace(" ","_").lower()
    renamed = renamed.replace(",","")
    renamed = renamed.replace("(","")
    renamed = renamed.replace(")","")
    os.rename(arg, renamed)
